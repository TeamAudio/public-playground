name: Publish GitHub Release Notes to website repo to create blog post
# Trigger when a new release changes from pre-release to latest

on:
  release:
    types: [released]
  # workflow_dispatch: # add manual trigger
  #   inputs:
  #     release_name:
  #       description: 'Test release name'
  #       required: true
  #       default: 'Test Release'
  #     release_body:
  #       description: 'Test release notes'
  #       required: true
  #       default: 'These are test release notes for manual triggering'

jobs:
  publish-release-notes:
    runs-on: ubuntu-latest
    # if: ${{ github.event.release.prerelease == false }} 

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          repository: TeamAudio/techaudio-web
          ref: 0746f99e656c88494491cfe2d3260285a4ff6967
          fetch-depth: 0  # Fetch all history for accurate versioning
          ssh-key: ${{ secrets.PUBLISH_RELEASE_SERVICE_KEY }}
          path: techaudio-web
      # - name: Call publish-release action
      #   uses: webfactory/ssh-agent@v0.9.1
      #   with:
      #     ssh-private-key: ${{ secrets.PUBLISH_RELEASE_SERVICE_KEY }}

      - name: Call publish-release action
        uses: ./techaudio-web/.github/actions/publish-release
        with:
          project-id: reaspeech-lite
          project-name: ReaSpeech Lite
          version-tag: ${{ github.event.release.tag_name || 'manual-test-tag' }}
          release-body: ${{ github.event.release.body }}

